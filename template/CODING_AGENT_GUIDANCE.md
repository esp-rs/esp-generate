#INCLUDEFILE option("coding-agent-guidance")
#INCLUDE_AS {coding-agent-guidance-file}
#REPLACE {coding-agent-guidance-file} coding-agent-guidance-file
# {coding-agent-guidance-file}

This project was generated by `esp-generate` for Espressif MCUs (`no_std`).

## Quick orientation

#REPLACE esp32c6 mcu
- Target chip: `esp32c6`
#REPLACE {generate-parameters} generate-parameters
- Generator parameters: `{generate-parameters}`
- Entry point:
  - `src/bin/main.rs` (blocking or Embassy async depending on selected options)
- Build/runner config:
  - `.cargo/config.toml`
- Dependencies and enabled features:
  - `Cargo.toml`
- Optional CI config:
  - `.github/workflows/rust_ci.yml`

## Common tasks

- Build/check: `cargo check`
- Format: `cargo fmt`
- Lint: `cargo clippy -- -D warnings`
- Run/flash: `cargo run`

The exact flash/debug backend is configured in `.cargo/config.toml` (typically `espflash` or `probe-rs`).

## Notes for coding agents

- Prefer minimal, targeted changes.
- Keep `no_std` constraints in mind.
- Respect selected feature flags and chip compatibility when editing dependencies or imports.
- If behavior depends on generation options, check generated comments in source files (`generator parameters:`).

## Useful references

- esp-generate: https://github.com/esp-rs/esp-generate
#REPLACE {current-version} esp-hal-version-full
- esp-hal docs: https://docs.espressif.com/projects/rust/esp-hal/{current-version}/index.html
#REPLACE {current-version} esp-hal-version-full
- esp-hal examples: https://github.com/esp-rs/esp-hal/tree/esp-hal-v{current-version}/examples
