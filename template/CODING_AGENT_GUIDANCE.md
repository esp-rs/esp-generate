#INCLUDEFILE option("coding-agent-guidance")
#INCLUDE_AS {coding-agent-guidance-file}
#REPLACE {coding-agent-guidance-file} coding-agent-guidance-file
# {coding-agent-guidance-file}

This project was generated by `esp-generate` for Espressif MCUs.

## Project-specific notes

#REPLACE esp32c6 mcu
- Target chip: `esp32c6`; keep dependencies, HAL features, and examples compatible with this chip.
- Check the `generator parameters:` comments in `src/bin/main.rs` before changing code that depends on selected generation options.
- This is a `#![no_std]` / `#![no_main]` project. Avoid `std`; use `core` and embedded-friendly crates.
#IF option("alloc")
- `alloc` is enabled. The heap is initialized in `src/bin/main.rs` with `esp_alloc::heap_allocator!`; adjust the heap size there if allocations fail.
#ELSE
- `alloc` is not enabled. Avoid heap-backed types such as `Vec`, `String`, `Box`, and boxed futures unless you add and initialize an allocator.
#ENDIF
- `.cargo/config.toml` contains the generated target, runner, build flags, and `[env]` defaults. Espressif crates expose additional configuration through environment variables; use `esp-config` to inspect/set those options. The linked "Additional configuration" sections for the enabled Espressif crates document crate-specific variables; keep project-specific defaults in `[env]` instead of hard-coding them in source.
#IF option("defmt")
- Logging uses `defmt`; the default filter is `DEFMT_LOG=info` in `.cargo/config.toml` and can be overridden from the environment.
#ELIF option("log")
- Logging uses `log`/`esp-println`; the default filter is `ESP_LOG=info` in `.cargo/config.toml` and can be overridden from the environment.
#ENDIF
#IF option("probe-rs")
- `cargo run` uses `probe-rs` as configured in `.cargo/config.toml`. This path is intended for RTT/defmt and debugging and requires a supported debug probe/JTAG connection, not a USB-UART-only serial adapter.
- If flashing or debugging fails, check `probe-rs list`, probe permissions/udev rules, board wiring/JTAG support, and that the `--chip` in `.cargo/config.toml` matches the target.
#ELSE
- `cargo run` uses `espflash` as configured in `.cargo/config.toml`. This path flashes through the board's serial/USB bootloader connection.
- If flashing fails, check bootloader mode, serial-port permissions, the selected port when multiple boards are connected, and that the `--chip` in `.cargo/config.toml` matches the target.
#ENDIF

## Useful references

- esp-generate: https://github.com/esp-rs/esp-generate
- esp-config: https://docs.espressif.com/projects/rust/esp-config/latest/esp_config/
#REPLACE 1.1.0 esp-hal-version-full
- esp-hal docs: https://docs.espressif.com/projects/rust/esp-hal/1.1.0/index.html
#REPLACE 1.1.0 esp-hal-version-full && esp32c6 mcu
- esp-hal additional configuration: https://docs.espressif.com/projects/rust/esp-hal/1.1.0/esp32c6/esp_hal/index.html#additional-configuration
#REPLACE esp32c6 mcu
- esp-bootloader-esp-idf additional configuration: https://docs.espressif.com/projects/rust/esp-bootloader-esp-idf/latest/esp32c6/esp_bootloader_esp_idf/index.html#additional-configuration
#IF option("embassy") || option("wifi") || option("ble-bleps") || option("ble-trouble")
#REPLACE esp32c6 mcu
- esp-rtos additional configuration: https://docs.espressif.com/projects/rust/esp-rtos/latest/esp32c6/esp_rtos/index.html#additional-configuration
#ENDIF
#IF option("esp-backtrace")
- esp-backtrace additional configuration: https://docs.espressif.com/projects/rust/esp-backtrace/latest/esp_backtrace/index.html#additional-configuration
#ENDIF
#IF option("wifi") || option("ble-bleps") || option("ble-trouble")
#REPLACE esp32c6 mcu
- esp-radio additional configuration: https://docs.espressif.com/projects/rust/esp-radio/latest/esp32c6/esp_radio/index.html#additional-configuration
#ENDIF
#REPLACE 1.1.0 esp-hal-version-full
- esp-hal examples: https://github.com/esp-rs/esp-hal/tree/esp-hal-v1.1.0/examples
